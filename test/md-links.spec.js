const read = require('../funtions');

describe ('read', () => {
  test('es una función', () => {
    expect(typeof read).toBe('object');
  })
})


/* describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });

}); */
